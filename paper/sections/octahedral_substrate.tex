% ============================================================
% octahedral_substrate.tex
% Section 2: The Octahedral Substrate
% ============================================================

\label{sec:substrate}

The $\Tdiamond$ lattice is a discrete topology: a graph in which each node
knows only which other nodes are adjacent to it.
No coordinates, no metric, no distances between non-adjacent nodes, and no
differential structure are assumed or built in.
The smooth spacetime of general relativity and quantum field theory is not
the stage on which the physics takes place --- it is a coarse-grained,
statistical description that emerges when the central limit theorem is
applied to many successive hops.
A particle that takes $N \gg 1$ steps has a position distribution that
converges to a Gaussian, and \emph{that} Gaussian lives on something that
looks like $\mathbb{R}^3$.
The manifold is the $N\to\infty$ limit of the graph, not the other way around.

This ordering matters.
Earlier discrete spacetime proposals~\cite{bombelli1987, thooft2016,
wolfram2002} typically begin with a continuum manifold and discretise it,
retaining coordinates and metric structure at the level of the fundamental
object.
Here there are no coordinates at the fundamental level.
The lattice is a graph topology, and spacetime emerges from it statistically.
The Dirac equation and the Einstein field equations are both large-$N$,
many-hop results; they are theorems about the graph, not inputs to it.

% ── 2.1 Nodes, edges, and causal adjacency ───────────────────────────────────
\subsection{Nodes, Edges, and Causal Adjacency}
\label{subsec:nodes}

A \emph{node} is a site: a location in the graph connected to neighbouring
sites by directed edges.
It carries no intrinsic geometry; all geometry is a consequence of the
adjacency structure.
A node has no knowledge of nodes two or more hops away --- locality is
exact, not approximate.

Two nodes are \emph{causally adjacent} if a signal propagating at the lattice
speed limit $c = 1$ node/tick can reach one from the other in a single tick.
The six directions of causal adjacency define the basis vectors of the lattice:
\begin{equation}
    \mathbf{V}_1 = (1,1,1),\quad
    \mathbf{V}_2 = (1,-1,-1),\quad
    \mathbf{V}_3 = (-1,1,-1),
    \label{eq:rgb_vectors}
\end{equation}
and their negatives $-\mathbf{V}_1$, $-\mathbf{V}_2$, $-\mathbf{V}_3$.
These are the six face-to-vertex diagonals of a unit cube, and they form
the edges of a regular octahedron centred at the origin.
The lattice $\Tdiamond$ is therefore the Cayley graph of the free abelian
group $\mathbb{Z}^3$ generated by these six vectors.

The hop distance --- the length of a single causal step --- is
\begin{equation}
    |\mathbf{V}_1| = \sqrt{1^2 + 1^2 + 1^2} = \sqrt{3}\;\ell_P,
    \label{eq:hop_distance}
\end{equation}
where $\ell_P$ is the Planck length, the natural calibration of the grid unit
(see Section~\ref{subsec:calibration}).
This is a body-diagonal of the unit cube, not a face-diagonal or an edge.
The geometry is octahedral, not cubic.

% ── 2.2 The bipartite structure ───────────────────────────────────────────────
\subsection{The Bipartite Structure and Chirality}
\label{subsec:bipartite}

The six basis vectors split naturally into two groups of three:
\begin{align}
    \text{RGB (even ticks):}\quad &\mathbf{V}_1,\;\mathbf{V}_2,\;\mathbf{V}_3,
    \label{eq:rgb}\\
    \text{CMY (odd ticks):}\quad  &-\mathbf{V}_1,\;-\mathbf{V}_2,\;-\mathbf{V}_3.
    \label{eq:cmy}
\end{align}
A particle on an \emph{even} node hops along RGB directions on even ticks and
is carried to an \emph{odd} node; from there it hops along CMY directions on
odd ticks and returns to an even node.
The lattice is therefore \emph{bipartite}: the even (RGB) and odd (CMY)
sublattices are never directly connected to each other within the same sublattice.

This bipartite structure has immediate physical consequences:

\begin{itemize}
\item \textbf{Massless propagation.}
A session with $\omega = 0$ (no phase mismatch) hops on every tick without
ever residing, alternating strictly between the two sublattices.
Its speed is exactly $c = 1$ node/tick, confirmed by \texttt{exp\_00}.

\item \textbf{Zitterbewegung.}
A massive session ($\omega > 0$) has a finite probability $p_\mathrm{stay} =
\sin^2(\omega/2)$ of remaining at its current node rather than hopping.
The resulting trembling motion between sublattices is the lattice realisation
of Schr\"odinger's \emph{Zitterbewegung}~\cite{schrodinger1930}.

\item \textbf{Spinor structure.}
The two sublattices carry the two spinor components of the Dirac field:
$\psi_R$ on the RGB sublattice (right-handed, even ticks) and $\psi_L$
on the CMY sublattice (left-handed, odd ticks).
The $\mathcal{A}=1$ constraint requires
$|\psi_R|^2 + |\psi_L|^2 = 1$ at every tick.
The Dirac equation emerges from this structure in the continuum limit
(Section~\ref{sec:kinematics}).
\end{itemize}

\begin{figure}[htbp]
    \centering
    \input{../figures/lattice_drawio.tex}
    \label{fig:lattice_structure}
\end{figure}

% ── 2.3 Same-sublattice exclusion ─────────────────────────────────────────────
\subsection{Same-Sublattice Exclusion}
\label{subsec:exclusion}

Because all hops are body-diagonal, two nodes on the same sublattice
(both even or both odd) are never directly connected.
The minimum distance between two even nodes is
\begin{equation}
    d_{\min}(\text{even--even})
        = |\mathbf{V}_1 - \mathbf{V}_2|
        = |(0,2,2)| = 2\sqrt{2}\;\ell_P.
    \label{eq:even_even}
\end{equation}
No causal path of length one exists between two same-sublattice nodes.
Two sessions of the same chirality --- two right-handed fermions, for
instance --- cannot occupy the same sublattice node or approach within
$2\sqrt{2}\;\ell_P$ of each other without violating the bipartite tick rule.

This geometric floor is the lattice analogue of the Pauli exclusion
principle.
Same-chirality occupation is forbidden by structure, not by postulate.

% ── 2.4 The causal cone ───────────────────────────────────────────────────────
\subsection{The Causal Cone}
\label{subsec:cone}

After $N$ ticks, a signal emitted from the origin can reach any node
$(a, b, c)$ for which a combination of the six basis vectors sums to
$(a, b, c)$ in exactly $N$ steps.
The set of reachable nodes after $N$ ticks forms an expanding octahedron
--- the \emph{causal cone} $\causalcone{N}$ --- with vertices at
$(\pm N, 0, 0)$, $(0, \pm N, 0)$, and $(0, 0, \pm N)$.

The number of distinct $N$-step paths from the origin to a node
$(a, b, c) \in \causalcone{N}$ is a pure combinatorial integer
$\pathcount{N}{a}{b}{c}$ derived in Section~\ref{sec:predictions}.
For large $N$ and fixed $(a,b,c)/N$, this count converges to a Gaussian
by the central limit theorem.
The deviations from the Gaussian at small $N$ are computable exactly and
constitute a falsifiable correction to the $1/r^2$ force law
(\texttt{exp\_06}).

% ── 2.5 Calibration ───────────────────────────────────────────────────────────
\subsection{Calibration and Physical Units}
\label{subsec:calibration}

The lattice has one free parameter: the identification of the grid unit with
a physical length.
We calibrate at the Planck scale: one grid unit $= \ell_P$.
The resulting unit table is shown in Figure~\ref{fig:diamond_grid_units}
and summarised here:

\begin{center}
\begin{tabular}{lll}
\hline
Quantity & Formula & Tier \\
\hline
Hop distance   & $\delta x = \sqrt{3}\;\ell_P$    & Planck calibration$^*$ \\
Speed limit    & $c = 1$ hop/tick                  & Geometric \\
Lattice HUP    & $\delta x \cdot \delta p = \hbar\pi$ & Brillouin-zone floor$^{**}$ \\
HUP saturation & $R_1 \cdot k_\text{Bohr} = 1 = \hbar$ & \texttt{exp\_12}$^\ddagger$ \\
Arnold tongue  & $\omega \cdot R_1 = \pi/3$        & \texttt{exp\_11/12}$^\dagger$ \\
\hline
\end{tabular}
\end{center}

\noindent
$^*$Theoretical identification of grid unit with $\ell_P$.
$^{**}$Derived from lattice Brillouin-zone cutoff at $|p| = \pi/\sqrt{3}$.
$^\ddagger$Two-body hydrogen experiment; 4 significant figures.
$^\dagger$3:1 Zitterbewegung/orbital resonance; dynamical result,
derivation in Section~\ref{sec:kinematics}.

\medskip
The HUP saturation entry $R_1 \cdot k_\text{Bohr} = 1$ deserves emphasis.
The Bohr orbit is precisely the minimum-uncertainty state on the lattice:
$\Delta r \cdot \Delta p = 1 \geq \tfrac{1}{2}$.
This falls out of the two-body dynamics without fine-tuning --- it is
the condition under which the orbital phase and the Zitterbewegung phase
first lock into a 3:1 Arnold tongue resonance.
Quantization is not imposed; it is the attractor.

\begin{figure}[htbp]
    \centering
    \input{../figures/Diamond_Grid_Units_drawio.tex}
    \label{fig:diamond_grid_units}
\end{figure}

% ── 2.6 Relation to prior work ────────────────────────────────────────────────
\subsection{Relation to Prior Discrete Spacetime Frameworks}
\label{subsec:prior_work}

The $\Tdiamond$ lattice belongs to a family of discrete spacetime
programmes that includes causal set theory~\cite{bombelli1987},
cellular automaton approaches~\cite{wolfram2002,thooft2016},
and earlier lattice quantum gravity work~\cite{zuse1969,wheeler1990}.

It differs from these in three respects.
First, the bipartite RGB/CMY structure is not assumed for algebraic
convenience --- it emerges from the requirement that the six causal
directions split into two chirally-opposite groups of three, which is
the unique solution for $\mathbb{Z}^3$ with coordination number six.
Second, the $\mathcal{A}=1$ constraint is applied \emph{per session}
rather than globally, making each particle a local conservation law
rather than a contribution to a global norm.
Third, and most importantly, the framework makes falsifiable numerical
predictions with no free parameters beyond the Planck calibration:
the discrete path-count correction to $1/r^2$ (\texttt{exp\_06}),
the four-significant-figure hydrogen Bohr radius (\texttt{exp\_12}),
and the minimum stable proton mass from two-body orbital dynamics
(\texttt{exp\_16}).
